{
  "name": "meshhire",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js",
    "db:migrate:local": "wrangler d1 migrations apply meshhire --local",
    "db:migrate:remote": "wrangler d1 migrations apply meshhire --remote"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
