import test from 'node:test';import assert from 'node:assert/strict';import {validationProductPayload,validationRequestPayload,validationResultPayload} from '../dist/validation.js';

test('validation SDK uses protocol signature domains',()=>{assert.equal(validationProductPayload({product_id:'p1',validator_passport_id:'agt_v',validation_type:'domain_control',title:'Domain check',payment_offer_id:'offer1',validity_days:90,issued_at:'2026-09-04T10:00:00Z'}).domain,'accordtrace.validation.product.v1');assert.equal(validationRequestPayload({request_id:'r1',product_id:'p1',subject_passport_id:'agt_s',validator_passport_id:'agt_v',validation_type:'domain_control',payment_order_id:'o1',subject_ref:'https://Example.com/path',requested_at:'2026-09-04T10:00:00Z'}).subject_ref,'example.com');});
test('passed validation cannot be built without evidence digest',()=>{assert.throws(()=>validationResultPayload({request_id:'r1',product_id:'p1',subject_passport_id:'agt_s',validator_passport_id:'agt_v',validation_type:'domain_control',outcome:'passed',evidence_digest:null,completed_at:'2026-09-04T10:00:00Z',expires_at:'2026-12-03T10:00:00Z'}),/evidence_digest/);});
test('failed validation may be represented without evidence digest',()=>{const p=validationResultPayload({request_id:'r1',product_id:'p1',subject_passport_id:'agt_s',validator_passport_id:'agt_v',validation_type:'security_assessment',outcome:'failed',evidence_digest:null,completed_at:'2026-09-04T10:00:00Z',expires_at:null});assert.equal(p.outcome,'failed');assert.equal(p.evidence_digest,null);});
