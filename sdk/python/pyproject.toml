[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "notary-protocol"
version = "0.1.0"
description = "Python client and protocol helpers for Notary Protocol."
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []
